import styled from '@emotion/styled';

export const PaymentIconListContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 6px;
`;

export const MoreText = styled.span`
  font-family: Helvetica, Arial, sans-serif;
  font-size: 14px;
  line-height: 1.5;
  font-weight: 700;
  color: var(--color-gray-80);
  text-decoration: underline;
  cursor: pointer;
`;
